default:
  production: TRUE
  verbose: FALSE
  name: "pca"
  title: "Principal Components Analysis"
  description: |
    This app allows to compute a principal components analysis based on the
    singular value decomposition.

test:
  production: FALSE
  verbose: TRUE
